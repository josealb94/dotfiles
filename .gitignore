# Configuración local (contiene paths al repo privado)
dotfiles.conf

# macOS
.DS_Store

# Backups generados por el instalador
*.bak

# Override OS-específico de Ghostty (symlink creado por ghostty.sh por máquina)
ghostty/.config/ghostty/config.local

# Override OS-específico de Kitty (symlink creado por kitty.sh por máquina)
kitty/.config/kitty/kitty.conf.local
